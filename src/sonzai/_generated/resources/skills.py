# Generated by scripts/codegen/generate_resources.py. Do not edit.
# Regenerate via `just regenerate-sdk`.
"""Auto-generated resource classes for the Skills tag."""

from __future__ import annotations

from typing import Any
from urllib.parse import quote
from sonzai._generated.models import (
    GetSkillLoadCountOutputBody,
    ListEnabledSkillsOutputBody,
    ListProjectSkillsOutputBody,
    ProjectSkillBody,
    Skill,
    ToggleEnabledSkillInputBody,
    ToggleEnabledSkillOutputBody,
    UpdateProjectSkillInputBody,
)
from sonzai._pagination import AsyncPage, Page
from sonzai._request_helpers import encode_body


class _SkillsBase:
    def __init__(self, http: Any) -> None:
        self._http = http


class Skills(_SkillsBase):
    def list_enabled_skills(
        self,
        agent_id: str,
    ) -> ListEnabledSkillsOutputBody:
        """List skills enabled for an agent"""
        path = f"/api/v1/agents/{quote(agent_id, safe='')}/skills/enabled"
        params = None
        data = self._http.get(path, params=params)
        return ListEnabledSkillsOutputBody.model_validate(data)

    def toggle_enabled_skill(
        self,
        agent_id: str,
        *,
        enabled: bool,
        skill_name: str,
    ) -> ToggleEnabledSkillOutputBody:
        """Toggle a skill's enabled flag for an agent"""
        path = f"/api/v1/agents/{quote(agent_id, safe='')}/skills/enabled"
        params = None
        _raw: dict[str, Any] = {}
        if enabled is not None:
            _raw["enabled"] = enabled
        if skill_name is not None:
            _raw["skill_name"] = skill_name
        body = encode_body(ToggleEnabledSkillInputBody, _raw)
        data = self._http.post(path, params=params, json_data=body)
        return ToggleEnabledSkillOutputBody.model_validate(data)

    def get_skill_load_count(
        self,
        agent_id: str,
        skill_name: str,
    ) -> GetSkillLoadCountOutputBody:
        """Read the per-agent load counter for a skill"""
        path = f"/api/v1/agents/{quote(agent_id, safe='')}/skills/enabled/{quote(skill_name, safe='')}/load-count"
        params = None
        data = self._http.get(path, params=params)
        return GetSkillLoadCountOutputBody.model_validate(data)

    def list_project_skills(
        self,
        project_id: str,
    ) -> ListProjectSkillsOutputBody:
        """List skills in a project library"""
        path = f"/api/v1/projects/{quote(project_id, safe='')}/skills"
        params = None
        data = self._http.get(path, params=params)
        return ListProjectSkillsOutputBody.model_validate(data)

    def create_project_skill(
        self,
        project_id: str,
        *,
        content: str,
        description: str,
        name: str,
        when_to_use: str | None = None,
    ) -> Skill:
        """Create or upsert a skill in a project library"""
        path = f"/api/v1/projects/{quote(project_id, safe='')}/skills"
        params = None
        _raw: dict[str, Any] = {}
        if content is not None:
            _raw["content"] = content
        if description is not None:
            _raw["description"] = description
        if name is not None:
            _raw["name"] = name
        if when_to_use is not None:
            _raw["when_to_use"] = when_to_use
        body = encode_body(ProjectSkillBody, _raw)
        data = self._http.post(path, params=params, json_data=body)
        return Skill.model_validate(data)

    def delete_project_skill(
        self,
        project_id: str,
        name: str,
    ) -> Any:
        """Remove a skill from a project library"""
        path = f"/api/v1/projects/{quote(project_id, safe='')}/skills/{quote(name, safe='')}"
        params = None
        data = self._http.delete(path, params=params)
        return data

    def get_project_skill(
        self,
        project_id: str,
        name: str,
    ) -> Skill:
        """Read a skill from a project library"""
        path = f"/api/v1/projects/{quote(project_id, safe='')}/skills/{quote(name, safe='')}"
        params = None
        data = self._http.get(path, params=params)
        return Skill.model_validate(data)

    def update_project_skill(
        self,
        project_id: str,
        name: str,
        *,
        content: str | None = None,
        description: str | None = None,
        when_to_use: str | None = None,
    ) -> Skill:
        """Update a skill in a project library"""
        path = f"/api/v1/projects/{quote(project_id, safe='')}/skills/{quote(name, safe='')}"
        params = None
        _raw: dict[str, Any] = {}
        if content is not None:
            _raw["content"] = content
        if description is not None:
            _raw["description"] = description
        if when_to_use is not None:
            _raw["when_to_use"] = when_to_use
        body = encode_body(UpdateProjectSkillInputBody, _raw)
        data = self._http.put(path, params=params, json_data=body)
        return Skill.model_validate(data)


class AsyncSkills(_SkillsBase):
    async def list_enabled_skills(
        self,
        agent_id: str,
    ) -> ListEnabledSkillsOutputBody:
        """List skills enabled for an agent"""
        path = f"/api/v1/agents/{quote(agent_id, safe='')}/skills/enabled"
        params = None
        data = await self._http.get(path, params=params)
        return ListEnabledSkillsOutputBody.model_validate(data)

    async def toggle_enabled_skill(
        self,
        agent_id: str,
        *,
        enabled: bool,
        skill_name: str,
    ) -> ToggleEnabledSkillOutputBody:
        """Toggle a skill's enabled flag for an agent"""
        path = f"/api/v1/agents/{quote(agent_id, safe='')}/skills/enabled"
        params = None
        _raw: dict[str, Any] = {}
        if enabled is not None:
            _raw["enabled"] = enabled
        if skill_name is not None:
            _raw["skill_name"] = skill_name
        body = encode_body(ToggleEnabledSkillInputBody, _raw)
        data = await self._http.post(path, params=params, json_data=body)
        return ToggleEnabledSkillOutputBody.model_validate(data)

    async def get_skill_load_count(
        self,
        agent_id: str,
        skill_name: str,
    ) -> GetSkillLoadCountOutputBody:
        """Read the per-agent load counter for a skill"""
        path = f"/api/v1/agents/{quote(agent_id, safe='')}/skills/enabled/{quote(skill_name, safe='')}/load-count"
        params = None
        data = await self._http.get(path, params=params)
        return GetSkillLoadCountOutputBody.model_validate(data)

    async def list_project_skills(
        self,
        project_id: str,
    ) -> ListProjectSkillsOutputBody:
        """List skills in a project library"""
        path = f"/api/v1/projects/{quote(project_id, safe='')}/skills"
        params = None
        data = await self._http.get(path, params=params)
        return ListProjectSkillsOutputBody.model_validate(data)

    async def create_project_skill(
        self,
        project_id: str,
        *,
        content: str,
        description: str,
        name: str,
        when_to_use: str | None = None,
    ) -> Skill:
        """Create or upsert a skill in a project library"""
        path = f"/api/v1/projects/{quote(project_id, safe='')}/skills"
        params = None
        _raw: dict[str, Any] = {}
        if content is not None:
            _raw["content"] = content
        if description is not None:
            _raw["description"] = description
        if name is not None:
            _raw["name"] = name
        if when_to_use is not None:
            _raw["when_to_use"] = when_to_use
        body = encode_body(ProjectSkillBody, _raw)
        data = await self._http.post(path, params=params, json_data=body)
        return Skill.model_validate(data)

    async def delete_project_skill(
        self,
        project_id: str,
        name: str,
    ) -> Any:
        """Remove a skill from a project library"""
        path = f"/api/v1/projects/{quote(project_id, safe='')}/skills/{quote(name, safe='')}"
        params = None
        data = await self._http.delete(path, params=params)
        return data

    async def get_project_skill(
        self,
        project_id: str,
        name: str,
    ) -> Skill:
        """Read a skill from a project library"""
        path = f"/api/v1/projects/{quote(project_id, safe='')}/skills/{quote(name, safe='')}"
        params = None
        data = await self._http.get(path, params=params)
        return Skill.model_validate(data)

    async def update_project_skill(
        self,
        project_id: str,
        name: str,
        *,
        content: str | None = None,
        description: str | None = None,
        when_to_use: str | None = None,
    ) -> Skill:
        """Update a skill in a project library"""
        path = f"/api/v1/projects/{quote(project_id, safe='')}/skills/{quote(name, safe='')}"
        params = None
        _raw: dict[str, Any] = {}
        if content is not None:
            _raw["content"] = content
        if description is not None:
            _raw["description"] = description
        if when_to_use is not None:
            _raw["when_to_use"] = when_to_use
        body = encode_body(UpdateProjectSkillInputBody, _raw)
        data = await self._http.put(path, params=params, json_data=body)
        return Skill.model_validate(data)
