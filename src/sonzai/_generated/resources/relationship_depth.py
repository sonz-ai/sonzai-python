# Generated by scripts/codegen/generate_resources.py. Do not edit.
# Regenerate via `just regenerate-sdk`.
"""Auto-generated resource classes for the RelationshipDepth tag."""

from __future__ import annotations

from typing import Any
from urllib.parse import quote
from sonzai._generated.models import (
    AtRiskContactsBody,
    CohortsBody,
    DepthBody,
    DepthDistributionBody,
    FleetActivityBody,
    ListProjectDepthScoresResponse,
    MoatIndexBody,
    PipelineFlowBody,
)
from sonzai._pagination import AsyncPage, Page
from sonzai._request_helpers import encode_body


class _RelationshipDepthBase:
    def __init__(self, http: Any) -> None:
        self._http = http


class RelationshipDepth(_RelationshipDepthBase):
    def get_contact_depth(
        self,
        agent_id: str,
        contact_id: str,
    ) -> DepthBody:
        """Get relationship depth for a contact"""
        path = (
            f"/api/v1/agents/{quote(agent_id, safe='')}/contacts/{quote(contact_id, safe='')}/depth"
        )
        params = None
        data = self._http.get(path, params=params)
        return DepthBody.model_validate(data)

    def get_agent_depth_distribution(
        self,
        agent_id: str,
    ) -> DepthDistributionBody:
        """Get depth distribution for an agent"""
        path = f"/api/v1/agents/{quote(agent_id, safe='')}/depth-distribution"
        params = None
        data = self._http.get(path, params=params)
        return DepthDistributionBody.model_validate(data)

    def get_project_at_risk_contacts(
        self,
        project_id: str,
        *,
        limit: int | None = None,
    ) -> AtRiskContactsBody:
        """Get at-risk contacts with recommended actions"""
        path = f"/api/v1/projects/{quote(project_id, safe='')}/at-risk"
        params: dict[str, Any] = {}
        if limit is not None:
            params["limit"] = limit
        data = self._http.get(path, params=params)
        return AtRiskContactsBody.model_validate(data)

    def get_project_cohorts(
        self,
        project_id: str,
    ) -> CohortsBody:
        """Get cohort retention data for a project"""
        path = f"/api/v1/projects/{quote(project_id, safe='')}/cohorts"
        params = None
        data = self._http.get(path, params=params)
        return CohortsBody.model_validate(data)

    def list_project_depth_scores(
        self,
        project_id: str,
        *,
        limit: int | None = None,
    ) -> ListProjectDepthScoresResponse:
        """List all depth scores for a project"""
        path = f"/api/v1/projects/{quote(project_id, safe='')}/depth-scores"
        params: dict[str, Any] = {}
        if limit is not None:
            params["limit"] = limit
        data = self._http.get(path, params=params)
        return ListProjectDepthScoresResponse.model_validate(data)

    def get_project_fleet_activity(
        self,
        project_id: str,
        *,
        limit: int | None = None,
    ) -> FleetActivityBody:
        """Get the live fleet activity feed for a project"""
        path = f"/api/v1/projects/{quote(project_id, safe='')}/fleet-activity"
        params: dict[str, Any] = {}
        if limit is not None:
            params["limit"] = limit
        data = self._http.get(path, params=params)
        return FleetActivityBody.model_validate(data)

    def get_project_moat(
        self,
        project_id: str,
    ) -> DepthDistributionBody:
        """Get the moat — aggregate relationship depth for a project"""
        path = f"/api/v1/projects/{quote(project_id, safe='')}/moat"
        params = None
        data = self._http.get(path, params=params)
        return DepthDistributionBody.model_validate(data)

    def get_project_moat_index(
        self,
        project_id: str,
    ) -> MoatIndexBody:
        """Get the composite Moat Index for a project"""
        path = f"/api/v1/projects/{quote(project_id, safe='')}/moat-index"
        params = None
        data = self._http.get(path, params=params)
        return MoatIndexBody.model_validate(data)

    def get_project_pipeline_flow(
        self,
        project_id: str,
    ) -> PipelineFlowBody:
        """Get the real pipeline flow for a project"""
        path = f"/api/v1/projects/{quote(project_id, safe='')}/pipeline-flow"
        params = None
        data = self._http.get(path, params=params)
        return PipelineFlowBody.model_validate(data)


class AsyncRelationshipDepth(_RelationshipDepthBase):
    async def get_contact_depth(
        self,
        agent_id: str,
        contact_id: str,
    ) -> DepthBody:
        """Get relationship depth for a contact"""
        path = (
            f"/api/v1/agents/{quote(agent_id, safe='')}/contacts/{quote(contact_id, safe='')}/depth"
        )
        params = None
        data = await self._http.get(path, params=params)
        return DepthBody.model_validate(data)

    async def get_agent_depth_distribution(
        self,
        agent_id: str,
    ) -> DepthDistributionBody:
        """Get depth distribution for an agent"""
        path = f"/api/v1/agents/{quote(agent_id, safe='')}/depth-distribution"
        params = None
        data = await self._http.get(path, params=params)
        return DepthDistributionBody.model_validate(data)

    async def get_project_at_risk_contacts(
        self,
        project_id: str,
        *,
        limit: int | None = None,
    ) -> AtRiskContactsBody:
        """Get at-risk contacts with recommended actions"""
        path = f"/api/v1/projects/{quote(project_id, safe='')}/at-risk"
        params: dict[str, Any] = {}
        if limit is not None:
            params["limit"] = limit
        data = await self._http.get(path, params=params)
        return AtRiskContactsBody.model_validate(data)

    async def get_project_cohorts(
        self,
        project_id: str,
    ) -> CohortsBody:
        """Get cohort retention data for a project"""
        path = f"/api/v1/projects/{quote(project_id, safe='')}/cohorts"
        params = None
        data = await self._http.get(path, params=params)
        return CohortsBody.model_validate(data)

    async def list_project_depth_scores(
        self,
        project_id: str,
        *,
        limit: int | None = None,
    ) -> ListProjectDepthScoresResponse:
        """List all depth scores for a project"""
        path = f"/api/v1/projects/{quote(project_id, safe='')}/depth-scores"
        params: dict[str, Any] = {}
        if limit is not None:
            params["limit"] = limit
        data = await self._http.get(path, params=params)
        return ListProjectDepthScoresResponse.model_validate(data)

    async def get_project_fleet_activity(
        self,
        project_id: str,
        *,
        limit: int | None = None,
    ) -> FleetActivityBody:
        """Get the live fleet activity feed for a project"""
        path = f"/api/v1/projects/{quote(project_id, safe='')}/fleet-activity"
        params: dict[str, Any] = {}
        if limit is not None:
            params["limit"] = limit
        data = await self._http.get(path, params=params)
        return FleetActivityBody.model_validate(data)

    async def get_project_moat(
        self,
        project_id: str,
    ) -> DepthDistributionBody:
        """Get the moat — aggregate relationship depth for a project"""
        path = f"/api/v1/projects/{quote(project_id, safe='')}/moat"
        params = None
        data = await self._http.get(path, params=params)
        return DepthDistributionBody.model_validate(data)

    async def get_project_moat_index(
        self,
        project_id: str,
    ) -> MoatIndexBody:
        """Get the composite Moat Index for a project"""
        path = f"/api/v1/projects/{quote(project_id, safe='')}/moat-index"
        params = None
        data = await self._http.get(path, params=params)
        return MoatIndexBody.model_validate(data)

    async def get_project_pipeline_flow(
        self,
        project_id: str,
    ) -> PipelineFlowBody:
        """Get the real pipeline flow for a project"""
        path = f"/api/v1/projects/{quote(project_id, safe='')}/pipeline-flow"
        params = None
        data = await self._http.get(path, params=params)
        return PipelineFlowBody.model_validate(data)
