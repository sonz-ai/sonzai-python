# Generated by scripts/codegen/generate_resources.py. Do not edit.
# Regenerate via `just regenerate-sdk`.
"""Auto-generated resource classes for the CustomAgents tag."""

from __future__ import annotations

from typing import Any
from urllib.parse import quote
from sonzai._generated.models import (
    CustomAgentDTO,
    CustomAgentWriteBody,
    ListCustomAgentsOutputBody,
)
from sonzai._pagination import AsyncPage, Page
from sonzai._request_helpers import encode_body


class _CustomAgentsBase:
    def __init__(self, http: Any) -> None:
        self._http = http


class CustomAgents(_CustomAgentsBase):
    def list_custom_agents(
        self,
    ) -> ListCustomAgentsOutputBody:
        """List custom agents"""
        path = f"/api/v1/custom-agents"
        params = None
        data = self._http.get(path, params=params)
        return ListCustomAgentsOutputBody.model_validate(data)

    def create_custom_agent(
        self,
        *,
        description: str | None = None,
        disable_tools: bool | None = None,
        findings_schema: dict[str, Any] | None = None,
        max_tool_rounds: int | None = None,
        model: str,
        name: str,
        slug: str,
        system: str,
        tools: list[Any] | None = None,
    ) -> Any:
        """Create a custom agent"""
        path = f"/api/v1/custom-agents"
        params = None
        _raw: dict[str, Any] = {}
        if description is not None:
            _raw["description"] = description
        if disable_tools is not None:
            _raw["disable_tools"] = disable_tools
        if findings_schema is not None:
            _raw["findings_schema"] = findings_schema
        if max_tool_rounds is not None:
            _raw["max_tool_rounds"] = max_tool_rounds
        if model is not None:
            _raw["model"] = model
        if name is not None:
            _raw["name"] = name
        if slug is not None:
            _raw["slug"] = slug
        if system is not None:
            _raw["system"] = system
        if tools is not None:
            _raw["tools"] = tools
        body = encode_body(CustomAgentWriteBody, _raw)
        data = self._http.post(path, params=params, json_data=body)
        return data

    def delete_custom_agent(
        self,
        agent_id: str,
    ) -> Any:
        """Delete a custom agent"""
        path = f"/api/v1/custom-agents/{quote(agent_id, safe='')}"
        params = None
        data = self._http.delete(path, params=params)
        return data

    def get_custom_agent(
        self,
        agent_id: str,
    ) -> CustomAgentDTO:
        """Get a custom agent"""
        path = f"/api/v1/custom-agents/{quote(agent_id, safe='')}"
        params = None
        data = self._http.get(path, params=params)
        return CustomAgentDTO.model_validate(data)

    def update_custom_agent(
        self,
        agent_id: str,
        *,
        description: str | None = None,
        disable_tools: bool | None = None,
        findings_schema: dict[str, Any] | None = None,
        max_tool_rounds: int | None = None,
        model: str,
        name: str,
        slug: str,
        system: str,
        tools: list[Any] | None = None,
    ) -> CustomAgentDTO:
        """Update a custom agent"""
        path = f"/api/v1/custom-agents/{quote(agent_id, safe='')}"
        params = None
        _raw: dict[str, Any] = {}
        if description is not None:
            _raw["description"] = description
        if disable_tools is not None:
            _raw["disable_tools"] = disable_tools
        if findings_schema is not None:
            _raw["findings_schema"] = findings_schema
        if max_tool_rounds is not None:
            _raw["max_tool_rounds"] = max_tool_rounds
        if model is not None:
            _raw["model"] = model
        if name is not None:
            _raw["name"] = name
        if slug is not None:
            _raw["slug"] = slug
        if system is not None:
            _raw["system"] = system
        if tools is not None:
            _raw["tools"] = tools
        body = encode_body(CustomAgentWriteBody, _raw)
        data = self._http.put(path, params=params, json_data=body)
        return CustomAgentDTO.model_validate(data)


class AsyncCustomAgents(_CustomAgentsBase):
    async def list_custom_agents(
        self,
    ) -> ListCustomAgentsOutputBody:
        """List custom agents"""
        path = f"/api/v1/custom-agents"
        params = None
        data = await self._http.get(path, params=params)
        return ListCustomAgentsOutputBody.model_validate(data)

    async def create_custom_agent(
        self,
        *,
        description: str | None = None,
        disable_tools: bool | None = None,
        findings_schema: dict[str, Any] | None = None,
        max_tool_rounds: int | None = None,
        model: str,
        name: str,
        slug: str,
        system: str,
        tools: list[Any] | None = None,
    ) -> Any:
        """Create a custom agent"""
        path = f"/api/v1/custom-agents"
        params = None
        _raw: dict[str, Any] = {}
        if description is not None:
            _raw["description"] = description
        if disable_tools is not None:
            _raw["disable_tools"] = disable_tools
        if findings_schema is not None:
            _raw["findings_schema"] = findings_schema
        if max_tool_rounds is not None:
            _raw["max_tool_rounds"] = max_tool_rounds
        if model is not None:
            _raw["model"] = model
        if name is not None:
            _raw["name"] = name
        if slug is not None:
            _raw["slug"] = slug
        if system is not None:
            _raw["system"] = system
        if tools is not None:
            _raw["tools"] = tools
        body = encode_body(CustomAgentWriteBody, _raw)
        data = await self._http.post(path, params=params, json_data=body)
        return data

    async def delete_custom_agent(
        self,
        agent_id: str,
    ) -> Any:
        """Delete a custom agent"""
        path = f"/api/v1/custom-agents/{quote(agent_id, safe='')}"
        params = None
        data = await self._http.delete(path, params=params)
        return data

    async def get_custom_agent(
        self,
        agent_id: str,
    ) -> CustomAgentDTO:
        """Get a custom agent"""
        path = f"/api/v1/custom-agents/{quote(agent_id, safe='')}"
        params = None
        data = await self._http.get(path, params=params)
        return CustomAgentDTO.model_validate(data)

    async def update_custom_agent(
        self,
        agent_id: str,
        *,
        description: str | None = None,
        disable_tools: bool | None = None,
        findings_schema: dict[str, Any] | None = None,
        max_tool_rounds: int | None = None,
        model: str,
        name: str,
        slug: str,
        system: str,
        tools: list[Any] | None = None,
    ) -> CustomAgentDTO:
        """Update a custom agent"""
        path = f"/api/v1/custom-agents/{quote(agent_id, safe='')}"
        params = None
        _raw: dict[str, Any] = {}
        if description is not None:
            _raw["description"] = description
        if disable_tools is not None:
            _raw["disable_tools"] = disable_tools
        if findings_schema is not None:
            _raw["findings_schema"] = findings_schema
        if max_tool_rounds is not None:
            _raw["max_tool_rounds"] = max_tool_rounds
        if model is not None:
            _raw["model"] = model
        if name is not None:
            _raw["name"] = name
        if slug is not None:
            _raw["slug"] = slug
        if system is not None:
            _raw["system"] = system
        if tools is not None:
            _raw["tools"] = tools
        body = encode_body(CustomAgentWriteBody, _raw)
        data = await self._http.put(path, params=params, json_data=body)
        return CustomAgentDTO.model_validate(data)
