# Generated by scripts/codegen/generate_resources.py. Do not edit.
# Regenerate via `just regenerate-sdk`.
"""Auto-generated resource classes for the Byok tag."""

from __future__ import annotations

from typing import Any
from urllib.parse import quote
from sonzai._generated.models import (
    BYOKKeyResponse,
    ListBYOKKeysOutputBody,
    PutBYOKKeyInputBody,
    SetBYOKActiveInputBody,
)
from sonzai._pagination import AsyncPage, Page
from sonzai._request_helpers import encode_body


class _ByokBase:
    def __init__(self, http: Any) -> None:
        self._http = http


class Byok(_ByokBase):
    def list_byok_keys(
        self,
        project_id: str,
    ) -> ListBYOKKeysOutputBody:
        """List BYOK keys for a project"""
        path = f"/api/v1/projects/{quote(project_id, safe='')}/byok-keys"
        params = None
        data = self._http.get(path, params=params)
        return ListBYOKKeysOutputBody.model_validate(data)

    def delete_byok_key(
        self,
        project_id: str,
        provider: str,
    ) -> Any:
        """Delete a BYOK key"""
        path = f"/api/v1/projects/{quote(project_id, safe='')}/byok-keys/{quote(provider, safe='')}"
        params = None
        data = self._http.delete(path, params=params)
        return data

    def set_byok_active(
        self,
        project_id: str,
        provider: str,
        *,
        is_active: bool,
    ) -> BYOKKeyResponse:
        """Enable or disable a BYOK key without rotating it"""
        path = f"/api/v1/projects/{quote(project_id, safe='')}/byok-keys/{quote(provider, safe='')}"
        params = None
        _raw: dict[str, Any] = {}
        if is_active is not None:
            _raw["is_active"] = is_active
        body = encode_body(SetBYOKActiveInputBody, _raw)
        data = self._http.patch(path, params=params, json_data=body)
        return BYOKKeyResponse.model_validate(data)

    def put_byok_key(
        self,
        project_id: str,
        provider: str,
        *,
        api_key: str,
    ) -> BYOKKeyResponse:
        """Create or replace a BYOK key"""
        path = f"/api/v1/projects/{quote(project_id, safe='')}/byok-keys/{quote(provider, safe='')}"
        params = None
        _raw: dict[str, Any] = {}
        if api_key is not None:
            _raw["api_key"] = api_key
        body = encode_body(PutBYOKKeyInputBody, _raw)
        data = self._http.put(path, params=params, json_data=body)
        return BYOKKeyResponse.model_validate(data)

    def test_byok_key(
        self,
        project_id: str,
        provider: str,
    ) -> BYOKKeyResponse:
        """Re-test a stored BYOK key against the provider"""
        path = f"/api/v1/projects/{quote(project_id, safe='')}/byok-keys/{quote(provider, safe='')}/test"
        params = None
        data = self._http.post(path, params=params)
        return BYOKKeyResponse.model_validate(data)


class AsyncByok(_ByokBase):
    async def list_byok_keys(
        self,
        project_id: str,
    ) -> ListBYOKKeysOutputBody:
        """List BYOK keys for a project"""
        path = f"/api/v1/projects/{quote(project_id, safe='')}/byok-keys"
        params = None
        data = await self._http.get(path, params=params)
        return ListBYOKKeysOutputBody.model_validate(data)

    async def delete_byok_key(
        self,
        project_id: str,
        provider: str,
    ) -> Any:
        """Delete a BYOK key"""
        path = f"/api/v1/projects/{quote(project_id, safe='')}/byok-keys/{quote(provider, safe='')}"
        params = None
        data = await self._http.delete(path, params=params)
        return data

    async def set_byok_active(
        self,
        project_id: str,
        provider: str,
        *,
        is_active: bool,
    ) -> BYOKKeyResponse:
        """Enable or disable a BYOK key without rotating it"""
        path = f"/api/v1/projects/{quote(project_id, safe='')}/byok-keys/{quote(provider, safe='')}"
        params = None
        _raw: dict[str, Any] = {}
        if is_active is not None:
            _raw["is_active"] = is_active
        body = encode_body(SetBYOKActiveInputBody, _raw)
        data = await self._http.patch(path, params=params, json_data=body)
        return BYOKKeyResponse.model_validate(data)

    async def put_byok_key(
        self,
        project_id: str,
        provider: str,
        *,
        api_key: str,
    ) -> BYOKKeyResponse:
        """Create or replace a BYOK key"""
        path = f"/api/v1/projects/{quote(project_id, safe='')}/byok-keys/{quote(provider, safe='')}"
        params = None
        _raw: dict[str, Any] = {}
        if api_key is not None:
            _raw["api_key"] = api_key
        body = encode_body(PutBYOKKeyInputBody, _raw)
        data = await self._http.put(path, params=params, json_data=body)
        return BYOKKeyResponse.model_validate(data)

    async def test_byok_key(
        self,
        project_id: str,
        provider: str,
    ) -> BYOKKeyResponse:
        """Re-test a stored BYOK key against the provider"""
        path = f"/api/v1/projects/{quote(project_id, safe='')}/byok-keys/{quote(provider, safe='')}/test"
        params = None
        data = await self._http.post(path, params=params)
        return BYOKKeyResponse.model_validate(data)
