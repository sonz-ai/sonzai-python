# Generated by scripts/codegen/generate_resources.py. Do not edit.
# Regenerate via `just regenerate-sdk`.
"""Auto-generated resource classes for the NotificationChannels tag."""

from __future__ import annotations

from typing import Any
from urllib.parse import quote
from sonzai._generated.models import (
    ChannelDeliverBody,
    ChannelDeliverOutputBody,
    ChannelDTO,
    ChannelWriteBody,
    ListChannelsOutputBody,
)
from sonzai._pagination import AsyncPage, Page
from sonzai._request_helpers import encode_body


class _NotificationChannelsBase:
    def __init__(self, http: Any) -> None:
        self._http = http


class NotificationChannels(_NotificationChannelsBase):
    def list_channels(
        self,
    ) -> ListChannelsOutputBody:
        """List notification channels"""
        path = f"/api/v1/channels"
        params = None
        data = self._http.get(path, params=params)
        return ListChannelsOutputBody.model_validate(data)

    def create_channel(
        self,
        *,
        active: bool | None = None,
        config: dict[str, Any] | None = None,
        events: list[Any] | None = None,
        filter: dict[str, Any] | None = None,
        name: str,
        type: str,
    ) -> Any:
        """Create a notification channel"""
        path = f"/api/v1/channels"
        params = None
        _raw: dict[str, Any] = {}
        if active is not None:
            _raw["active"] = active
        if config is not None:
            _raw["config"] = config
        if events is not None:
            _raw["events"] = events
        if filter is not None:
            _raw["filter"] = filter
        if name is not None:
            _raw["name"] = name
        if type is not None:
            _raw["type"] = type
        body = encode_body(ChannelWriteBody, _raw)
        data = self._http.post(path, params=params, json_data=body)
        return data

    def deliver_to_channels(
        self,
        *,
        event_type: str,
        payload: dict[str, Any] | None = None,
    ) -> ChannelDeliverOutputBody:
        """Deliver an event to notification channels"""
        path = f"/api/v1/channels/deliver"
        params = None
        _raw: dict[str, Any] = {}
        if event_type is not None:
            _raw["event_type"] = event_type
        if payload is not None:
            _raw["payload"] = payload
        body = encode_body(ChannelDeliverBody, _raw)
        data = self._http.post(path, params=params, json_data=body)
        return ChannelDeliverOutputBody.model_validate(data)

    def delete_channel(
        self,
        channel_id: str,
    ) -> Any:
        """Delete a notification channel"""
        path = f"/api/v1/channels/{quote(channel_id, safe='')}"
        params = None
        data = self._http.delete(path, params=params)
        return data

    def get_channel(
        self,
        channel_id: str,
    ) -> ChannelDTO:
        """Get a notification channel"""
        path = f"/api/v1/channels/{quote(channel_id, safe='')}"
        params = None
        data = self._http.get(path, params=params)
        return ChannelDTO.model_validate(data)

    def update_channel(
        self,
        channel_id: str,
        *,
        active: bool | None = None,
        config: dict[str, Any] | None = None,
        events: list[Any] | None = None,
        filter: dict[str, Any] | None = None,
        name: str,
        type: str,
    ) -> ChannelDTO:
        """Update a notification channel"""
        path = f"/api/v1/channels/{quote(channel_id, safe='')}"
        params = None
        _raw: dict[str, Any] = {}
        if active is not None:
            _raw["active"] = active
        if config is not None:
            _raw["config"] = config
        if events is not None:
            _raw["events"] = events
        if filter is not None:
            _raw["filter"] = filter
        if name is not None:
            _raw["name"] = name
        if type is not None:
            _raw["type"] = type
        body = encode_body(ChannelWriteBody, _raw)
        data = self._http.put(path, params=params, json_data=body)
        return ChannelDTO.model_validate(data)

    def list_channels_for_project(
        self,
        project_id: str,
    ) -> ListChannelsOutputBody:
        """List notification channels for a project"""
        path = f"/api/v1/projects/{quote(project_id, safe='')}/channels"
        params = None
        data = self._http.get(path, params=params)
        return ListChannelsOutputBody.model_validate(data)

    def create_channel_for_project(
        self,
        project_id: str,
        *,
        active: bool | None = None,
        config: dict[str, Any] | None = None,
        events: list[Any] | None = None,
        filter: dict[str, Any] | None = None,
        name: str,
        type: str,
    ) -> Any:
        """Create a notification channel for a project"""
        path = f"/api/v1/projects/{quote(project_id, safe='')}/channels"
        params = None
        _raw: dict[str, Any] = {}
        if active is not None:
            _raw["active"] = active
        if config is not None:
            _raw["config"] = config
        if events is not None:
            _raw["events"] = events
        if filter is not None:
            _raw["filter"] = filter
        if name is not None:
            _raw["name"] = name
        if type is not None:
            _raw["type"] = type
        body = encode_body(ChannelWriteBody, _raw)
        data = self._http.post(path, params=params, json_data=body)
        return data

    def deliver_to_channels_for_project(
        self,
        project_id: str,
        *,
        event_type: str,
        payload: dict[str, Any] | None = None,
    ) -> ChannelDeliverOutputBody:
        """Deliver an event to project notification channels"""
        path = f"/api/v1/projects/{quote(project_id, safe='')}/channels/deliver"
        params = None
        _raw: dict[str, Any] = {}
        if event_type is not None:
            _raw["event_type"] = event_type
        if payload is not None:
            _raw["payload"] = payload
        body = encode_body(ChannelDeliverBody, _raw)
        data = self._http.post(path, params=params, json_data=body)
        return ChannelDeliverOutputBody.model_validate(data)

    def delete_channel_for_project(
        self,
        project_id: str,
        channel_id: str,
    ) -> Any:
        """Delete a notification channel for a project"""
        path = (
            f"/api/v1/projects/{quote(project_id, safe='')}/channels/{quote(channel_id, safe='')}"
        )
        params = None
        data = self._http.delete(path, params=params)
        return data

    def get_channel_for_project(
        self,
        project_id: str,
        channel_id: str,
    ) -> ChannelDTO:
        """Get a notification channel for a project"""
        path = (
            f"/api/v1/projects/{quote(project_id, safe='')}/channels/{quote(channel_id, safe='')}"
        )
        params = None
        data = self._http.get(path, params=params)
        return ChannelDTO.model_validate(data)

    def update_channel_for_project(
        self,
        project_id: str,
        channel_id: str,
        *,
        active: bool | None = None,
        config: dict[str, Any] | None = None,
        events: list[Any] | None = None,
        filter: dict[str, Any] | None = None,
        name: str,
        type: str,
    ) -> ChannelDTO:
        """Update a notification channel for a project"""
        path = (
            f"/api/v1/projects/{quote(project_id, safe='')}/channels/{quote(channel_id, safe='')}"
        )
        params = None
        _raw: dict[str, Any] = {}
        if active is not None:
            _raw["active"] = active
        if config is not None:
            _raw["config"] = config
        if events is not None:
            _raw["events"] = events
        if filter is not None:
            _raw["filter"] = filter
        if name is not None:
            _raw["name"] = name
        if type is not None:
            _raw["type"] = type
        body = encode_body(ChannelWriteBody, _raw)
        data = self._http.put(path, params=params, json_data=body)
        return ChannelDTO.model_validate(data)


class AsyncNotificationChannels(_NotificationChannelsBase):
    async def list_channels(
        self,
    ) -> ListChannelsOutputBody:
        """List notification channels"""
        path = f"/api/v1/channels"
        params = None
        data = await self._http.get(path, params=params)
        return ListChannelsOutputBody.model_validate(data)

    async def create_channel(
        self,
        *,
        active: bool | None = None,
        config: dict[str, Any] | None = None,
        events: list[Any] | None = None,
        filter: dict[str, Any] | None = None,
        name: str,
        type: str,
    ) -> Any:
        """Create a notification channel"""
        path = f"/api/v1/channels"
        params = None
        _raw: dict[str, Any] = {}
        if active is not None:
            _raw["active"] = active
        if config is not None:
            _raw["config"] = config
        if events is not None:
            _raw["events"] = events
        if filter is not None:
            _raw["filter"] = filter
        if name is not None:
            _raw["name"] = name
        if type is not None:
            _raw["type"] = type
        body = encode_body(ChannelWriteBody, _raw)
        data = await self._http.post(path, params=params, json_data=body)
        return data

    async def deliver_to_channels(
        self,
        *,
        event_type: str,
        payload: dict[str, Any] | None = None,
    ) -> ChannelDeliverOutputBody:
        """Deliver an event to notification channels"""
        path = f"/api/v1/channels/deliver"
        params = None
        _raw: dict[str, Any] = {}
        if event_type is not None:
            _raw["event_type"] = event_type
        if payload is not None:
            _raw["payload"] = payload
        body = encode_body(ChannelDeliverBody, _raw)
        data = await self._http.post(path, params=params, json_data=body)
        return ChannelDeliverOutputBody.model_validate(data)

    async def delete_channel(
        self,
        channel_id: str,
    ) -> Any:
        """Delete a notification channel"""
        path = f"/api/v1/channels/{quote(channel_id, safe='')}"
        params = None
        data = await self._http.delete(path, params=params)
        return data

    async def get_channel(
        self,
        channel_id: str,
    ) -> ChannelDTO:
        """Get a notification channel"""
        path = f"/api/v1/channels/{quote(channel_id, safe='')}"
        params = None
        data = await self._http.get(path, params=params)
        return ChannelDTO.model_validate(data)

    async def update_channel(
        self,
        channel_id: str,
        *,
        active: bool | None = None,
        config: dict[str, Any] | None = None,
        events: list[Any] | None = None,
        filter: dict[str, Any] | None = None,
        name: str,
        type: str,
    ) -> ChannelDTO:
        """Update a notification channel"""
        path = f"/api/v1/channels/{quote(channel_id, safe='')}"
        params = None
        _raw: dict[str, Any] = {}
        if active is not None:
            _raw["active"] = active
        if config is not None:
            _raw["config"] = config
        if events is not None:
            _raw["events"] = events
        if filter is not None:
            _raw["filter"] = filter
        if name is not None:
            _raw["name"] = name
        if type is not None:
            _raw["type"] = type
        body = encode_body(ChannelWriteBody, _raw)
        data = await self._http.put(path, params=params, json_data=body)
        return ChannelDTO.model_validate(data)

    async def list_channels_for_project(
        self,
        project_id: str,
    ) -> ListChannelsOutputBody:
        """List notification channels for a project"""
        path = f"/api/v1/projects/{quote(project_id, safe='')}/channels"
        params = None
        data = await self._http.get(path, params=params)
        return ListChannelsOutputBody.model_validate(data)

    async def create_channel_for_project(
        self,
        project_id: str,
        *,
        active: bool | None = None,
        config: dict[str, Any] | None = None,
        events: list[Any] | None = None,
        filter: dict[str, Any] | None = None,
        name: str,
        type: str,
    ) -> Any:
        """Create a notification channel for a project"""
        path = f"/api/v1/projects/{quote(project_id, safe='')}/channels"
        params = None
        _raw: dict[str, Any] = {}
        if active is not None:
            _raw["active"] = active
        if config is not None:
            _raw["config"] = config
        if events is not None:
            _raw["events"] = events
        if filter is not None:
            _raw["filter"] = filter
        if name is not None:
            _raw["name"] = name
        if type is not None:
            _raw["type"] = type
        body = encode_body(ChannelWriteBody, _raw)
        data = await self._http.post(path, params=params, json_data=body)
        return data

    async def deliver_to_channels_for_project(
        self,
        project_id: str,
        *,
        event_type: str,
        payload: dict[str, Any] | None = None,
    ) -> ChannelDeliverOutputBody:
        """Deliver an event to project notification channels"""
        path = f"/api/v1/projects/{quote(project_id, safe='')}/channels/deliver"
        params = None
        _raw: dict[str, Any] = {}
        if event_type is not None:
            _raw["event_type"] = event_type
        if payload is not None:
            _raw["payload"] = payload
        body = encode_body(ChannelDeliverBody, _raw)
        data = await self._http.post(path, params=params, json_data=body)
        return ChannelDeliverOutputBody.model_validate(data)

    async def delete_channel_for_project(
        self,
        project_id: str,
        channel_id: str,
    ) -> Any:
        """Delete a notification channel for a project"""
        path = (
            f"/api/v1/projects/{quote(project_id, safe='')}/channels/{quote(channel_id, safe='')}"
        )
        params = None
        data = await self._http.delete(path, params=params)
        return data

    async def get_channel_for_project(
        self,
        project_id: str,
        channel_id: str,
    ) -> ChannelDTO:
        """Get a notification channel for a project"""
        path = (
            f"/api/v1/projects/{quote(project_id, safe='')}/channels/{quote(channel_id, safe='')}"
        )
        params = None
        data = await self._http.get(path, params=params)
        return ChannelDTO.model_validate(data)

    async def update_channel_for_project(
        self,
        project_id: str,
        channel_id: str,
        *,
        active: bool | None = None,
        config: dict[str, Any] | None = None,
        events: list[Any] | None = None,
        filter: dict[str, Any] | None = None,
        name: str,
        type: str,
    ) -> ChannelDTO:
        """Update a notification channel for a project"""
        path = (
            f"/api/v1/projects/{quote(project_id, safe='')}/channels/{quote(channel_id, safe='')}"
        )
        params = None
        _raw: dict[str, Any] = {}
        if active is not None:
            _raw["active"] = active
        if config is not None:
            _raw["config"] = config
        if events is not None:
            _raw["events"] = events
        if filter is not None:
            _raw["filter"] = filter
        if name is not None:
            _raw["name"] = name
        if type is not None:
            _raw["type"] = type
        body = encode_body(ChannelWriteBody, _raw)
        data = await self._http.put(path, params=params, json_data=body)
        return ChannelDTO.model_validate(data)
